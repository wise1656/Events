import { Link } from 'react-router-dom';

const Empty = () => {
    return (
        <div>
            <div>Empty</div>
            <Link to='/'>Go back</Link>
        </div>
    );
};

export default Empty;
